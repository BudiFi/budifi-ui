import {
    colors,
    fontFamily,
    borderRadius,
    border,
    media,
    spacing,
    fontSize,
} from "./declarations";

const theme = {
    colors,
    media,
    fontFamily,
    borderRadius,
    fontSize,
    spacing,
    border,
};

export default theme;
