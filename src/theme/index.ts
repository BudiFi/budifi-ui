import {
    colors,
    fontFamily,
    borderRadius,
    border,
    media,
    spacing,
    fontSize,
    fontWeights,
} from "./declarations";
import { CustomTheme } from "./interfaces/styled";

const theme: CustomTheme = {
    colors,
    media,
    fontFamily,
    borderRadius,
    fontSize,
    spacing,
    border,
    fontWeights,
};

export default theme;
